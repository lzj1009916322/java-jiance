<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.weservice.catering.wtakeout.modules.app.dao.PointNotesDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.weservice.catering.wtakeout.modules.app.entity.PointNotesEntity" id="pointNotesMap">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="point" column="point"/>
<!--        <result property="prodcutId" column="prodcut_id"/>-->
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    <update id="update">
        update point_notes
        <trim prefix="set" suffixOverrides=",">
            <if test="point!=null ">point=#{point},</if>
            <if test="status!=null and status!=''">status=#{status},</if>
            <if test="money!=null">money=#{money},</if>
            <if test="updateTime!=null">update_time=#{updateTime},</if>
        </trim>
        WHERE id=#{id}
    </update>

</mapper>